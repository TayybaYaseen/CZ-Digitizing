# Local dev infra (docker-compose.yml) — copy this file to .env before running `docker compose up`
POSTGRES_DB=czdigitizing
POSTGRES_USER=dev
POSTGRES_PASSWORD=dev
POSTGRES_PORT=5432

REDIS_PORT=6379

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001

# Per-app env files: apps/api/.env.example, apps/web/.env.example, apps/admin/.env.example
