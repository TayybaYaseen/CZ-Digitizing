import { Module } from '@nestjs/common';

// Empty shell — implementation lands with
// docs/specs/2026-08-28-01-auth-account-security.md
// (register/login/2FA/new-device verification/forgot-password controllers,
// services, guards, and DTOs).
@Module({})
export class AuthModule {}
