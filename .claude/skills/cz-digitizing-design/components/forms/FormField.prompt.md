Wraps any control with its label, hint and error. The gold asterisk is how the quote form marks required fields.

```jsx
<FormField label="Full Name" required><Input placeholder="Full Name" /></FormField>
```
