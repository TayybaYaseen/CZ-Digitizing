Pill switch, gold active segment on a sunken track.

```jsx
<SegmentedToggle options={['Subscription Plans','Buy Credits']} value={mode} onChange={setMode} />
<SegmentedToggle size="sm" options={['Week','Month','Year']} value="Month" onChange={setRange} />
```
