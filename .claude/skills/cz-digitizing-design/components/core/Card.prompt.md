The standard surface — white, hairline grey border, 10px radius, `--shadow-sm`. Never a coloured left border.

```jsx
<Card title="Sales Overview" action={<SegmentedToggle options={['Week','Month','Year']} value="Month" />}>
  …
</Card>
```

`tone="navy"` for dark feature panels, `tone="gold"` for the soft upsell strip, `tone="flat"` inside already-elevated containers.
