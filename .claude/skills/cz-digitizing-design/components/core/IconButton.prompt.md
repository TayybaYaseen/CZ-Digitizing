Icon-only control. Used in the admin topbar (bell, with `badge`), table row actions and card headers.

```jsx
<IconButton icon="bell" label="Notifications" badge={3} />
<IconButton icon="trash-2" label="Remove" variant="outline" />
```
